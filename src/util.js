import { addView } from './util/add-view'
import { Command } from './util/command'
import { compare } from './util/compare'
import { detectTouch, detectMac } from './util/detect'
import { error } from './util/error'
import { Inflector } from './util/inflector'
import { makeSortFn } from './util/make-sort-fn'
import { ordinalize } from './util/ordinalize'
import { pluralize } from './util/pluralize'
import { Popup, makePopup } from './util/popup'
import { presence } from './util/presence'
import { reEscape } from './util/re-escape'
import { singularize } from './util/singularize'
import { Storage } from './util/storage'
import { stringToProc } from './util/string-to-proc'
import { template } from './util/template'
import { toProc } from './util/to-proc'
import { trim } from './util/trim'
import { ucFirst } from './util/uc-first'

export {
  addView,
  Command,
  compare,
  detectMac,
  detectTouch,
  error,
  Inflector,
  makeSortFn,
  ordinalize,
  pluralize,
  Popup,
  makePopup,
  presence,
  reEscape,
  singularize,
  Storage,
  stringToProc,
  template,
  toProc,
  trim,
  ucFirst
}
